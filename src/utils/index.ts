/**
 * ユーティリティ関数のエントリーポイント
 *
 * @module utils
 *
 * @description
 * アプリケーション全体で使用する共通ユーティリティ関数を提供。
 * スタイル操作、日付フォーマット、数値フォーマット、ロギングなどの機能を含む。
 *
 * @example
 * ```typescript
 * import { cn, formatDate, formatCurrency, logger } from '@/utils'
 *
 * // クラス名の結合
 * const className = cn('base-class', isActive && 'active')
 *
 * // 日付フォーマット
 * const formattedDate = formatDate(new Date())
 *
 * // 通貨フォーマット
 * const price = formatCurrency(1000)
 *
 * // ロギング
 * logger.info('処理完了')
 * ```
 */

// ================================================================================
// スタイル関連
// ================================================================================

export { cn } from "./cn";

// ================================================================================
// 日付操作
// ================================================================================

export { formatDate, formatDateJa, formatRelativeTime, parseDate } from "./date";

// ================================================================================
// フォーマット
// ================================================================================

export { formatCurrency, formatNumber } from "./format";

// ================================================================================
// ロギング
// ================================================================================

export { logger } from "./logger";
