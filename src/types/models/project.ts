// ================================================================================
// Zodスキーマのエクスポート
// ================================================================================

/**
 * Zodスキーマをエクスポート
 *
 * バリデーションが必要な場合は、これらのスキーマを使用してください。
 */
export { projectSchema } from "../schemas/project";

// ================================================================================
// 型定義
// ================================================================================

/**
 * プロジェクト型
 *
 * アプリケーション全体で使用する統一Project型定義
 *
 * バリデーションが必要な場合は projectSchema を使用してください。
 */
export type Project = {
  id: string;
  name: string;
  description: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
  created_by: string;
};
