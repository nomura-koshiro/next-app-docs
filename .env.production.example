# ================================================================================
# 本番環境用環境変数設定
# ================================================================================
# このファイルは本番環境用の設定例です。
# CI/CDパイプラインまたはホスティングサービスの環境変数設定で使用してください。
#
# 使用方法:
# 1. ホスティングサービス（Vercel, Azure, AWS等）の環境変数設定画面で設定
# 2. または `.env.production` ファイルを作成して設定（gitにコミットしないこと）
# ================================================================================

# ================================================================================
# API設定
# ================================================================================
# 本番環境のバックエンドAPIのベースURL
# 例: https://api.example.com/api/v1
NEXT_PUBLIC_API_URL=https://your-production-api.example.com/api/v1

# ================================================================================
# MSW (Mock Service Worker) 設定
# ================================================================================
# 本番環境では必ずMSWを無効化してください
# - false: 実際のバックエンドAPIを使用（必須）
NEXT_PUBLIC_ENABLE_API_MOCKING=false

# ================================================================================
# アプリケーション設定
# ================================================================================
# 本番環境のアプリケーションURL
# 例: https://example.com
NEXT_PUBLIC_URL=https://your-production-app.example.com

# モックAPIサーバーのポート番号（本番環境では不要）
NEXT_PUBLIC_MOCK_API_PORT=8080

# ================================================================================
# Azure Entra ID 認証設定
# ================================================================================

# 認証モード設定
# 本番環境では必ずAzure AD認証を使用
# - production: Azure Entra ID認証（MSAL使用）
NEXT_PUBLIC_AUTH_MODE=production

# ================================================================================
# Azure AD設定（必須）
# ================================================================================
# Azure Portalで本番環境用のアプリを登録してください
# https://portal.azure.com/#view/Microsoft_AAD_IAM/ActiveDirectoryMenuBlade/~/RegisteredApps
#
# 設定手順:
# 1. Azure Portalで新しいアプリを登録（本番環境用）
# 2. 「認証」セクションでリダイレクトURIを設定
# 3. 「APIのアクセス許可」セクションで必要な権限を設定
# 4. 以下の値をコピーして設定

# Azure ADアプリケーションID（クライアントID）
# Azure Portal > アプリの登録 > 概要 > アプリケーション (クライアント) ID
NEXT_PUBLIC_AZURE_CLIENT_ID=your-production-client-id-here

# Azure ADテナントID（ディレクトリID）
# Azure Portal > アプリの登録 > 概要 > ディレクトリ (テナント) ID
NEXT_PUBLIC_AZURE_TENANT_ID=your-production-tenant-id-here

# 認証後のリダイレクトURI
# Azure Portal > アプリの登録 > 認証 > リダイレクトURI に登録したURLと一致させる
# 例: https://example.com
NEXT_PUBLIC_AZURE_REDIRECT_URI=https://your-production-app.example.com

# ================================================================================
# セキュリティに関する重要な注意事項
# ================================================================================
# - 実際の値は絶対にGitにコミットしないでください
# - CI/CDパイプラインまたはホスティングサービスのシークレット管理機能を使用してください
# - Azure ADの設定は環境ごとに分けることを強く推奨します
# - 本番環境では以下のセキュリティ設定を確認してください:
#   * HTTPSの使用（必須）
#   * CORS設定の厳格化
#   * CSP（Content Security Policy）の設定
#   * Azure ADの条件付きアクセスポリシーの設定
# ================================================================================
