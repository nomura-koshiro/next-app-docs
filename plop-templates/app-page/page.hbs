import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "{{title}} | Sample App",
  description: "{{description}}",
};

export { default } from "@/features/{{kebabCase featureName}}/routes/{{kebabCase routeName}}";
