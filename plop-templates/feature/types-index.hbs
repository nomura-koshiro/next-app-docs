/**
 * {{pascalCase featureName}}の型定義
 *
 * TODO: 必要なフィールドを追加してください
 */
export type {{pascalCase featureName}} = {
  id: string;
  name: string;
  createdAt?: string;
  updatedAt?: string;
};

/**
 * {{pascalCase featureName}}作成時の入力型
 *
 * TODO: 必要なフィールドを追加してください
 */
export type Create{{pascalCase featureName}}Input = {
  name: string;
};

/**
 * {{pascalCase featureName}}更新時の入力型
 *
 * TODO: 必要なフィールドを追加してください
 */
export type Update{{pascalCase featureName}}Input = {
  name: string;
};
