import { z } from "zod";

/**
 * {{pascalCase featureName}}のバリデーションスキーマ
 */
export const {{camelCase featureName}}Schema = z.object({
  name: z.string().min(1, { message: "名前は必須です" }),
  // TODO: 必要なフィールドを追加してください
});

export type {{pascalCase featureName}}Values = z.infer<typeof {{camelCase featureName}}Schema>;
