"use client";

import { {{pascalCase routeName}}Form } from "./components/{{kebabCase routeName}}-form";
import { use{{pascalCase routeName}} } from "./{{kebabCase routeName}}.hook";

/**
 * {{pascalCase routeName}}ページコンテナ
 *
 * ロジックを use{{pascalCase routeName}} フックで管理し、
 * プレゼンテーショナルコンポーネント {{pascalCase routeName}}Form に渡します。
 */
export default function {{pascalCase routeName}}Page() {
  const { form, handleSubmit } = use{{pascalCase routeName}}();

  return (
    <{{pascalCase routeName}}Form
      control={form.control}
      onSubmit={handleSubmit}
      onReset={() => form.reset()}
      isSubmitting={form.formState.isSubmitting}
    />
  );
}
