import type { Meta, StoryObj } from "@storybook/react";
import { {{pascalCase componentName}} } from "./{{kebabCase componentName}}";

const meta = {
  title: "Components/Layout/{{pascalCase componentName}}",
  component: {{pascalCase componentName}},
  parameters: {
    layout: "fullscreen",
  },
  tags: ["autodocs"],
  argTypes: {
    children: {
      control: "text",
      description: "コンポーネントの子要素",
    },
  },
} satisfies Meta<typeof {{pascalCase componentName}}>;

export default meta;
type Story = StoryObj<typeof meta>;

/**
 * デフォルト表示
 */
export const Default: Story = {
  args: {
    children: (
      <div className="p-8">
        <h1 className="text-2xl font-bold mb-4">{{pascalCase componentName}}</h1>
        <p>レイアウトコンポーネントのサンプルコンテンツ</p>
      </div>
    ),
  },
};

/**
 * 複数の子要素
 */
export const MultipleChildren: Story = {
  args: {
    children: (
      <>
        <div className="p-4 bg-blue-50">セクション1</div>
        <div className="p-4 bg-green-50">セクション2</div>
        <div className="p-4 bg-yellow-50">セクション3</div>
      </>
    ),
  },
};
